{
  "name": "crm",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "api:generate": "pnpm --filter @workspace/api-client run generate",
    "api:generate:watch": "pnpm --filter @workspace/api-client run generate:watch"
  },
  "keywords": [],
  "author": "nmbrsdntl",
  "license": "ISC",
  "packageManager": "pnpm@10.12.1",
  "dependencies": {
    "next": "^15.5.7",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-redux": "^9.2.0"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
